const frist = `Abid`;
const last = `nabid`;


// const fullname = frist + last;

const fullname = frist + ' ' + last;

// console.log(fullname);
const fullName2 = frist.concat(last);
console.log(fullName2);